<div id="home" class="tab-pane active">
  <div class="col-md-9 col-sm-9 col-xs-12" data-step="9" data-intro="Everything will display here" data-position='right'>
    <% if !@is_organizer %>
        <%= render 'conferences/panels/recommendations' %>
    <% else %>
        <%= render 'posts/new' %>
    <% end %>

    <%= render 'conferences/panels/schedule' %>
    <%= render 'conferences/panels/papers' %>
    <%= render 'conferences/panels/posts' %>
  </div>
  <div class="col-md-3 col-sm-3 col-xs-12 hidden-xs">
    <%= render 'conferences/panels/about' %>
  </div>
</div>

<%= render 'conferences/footer', scheduler: true %>