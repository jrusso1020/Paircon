<div class="x_panel">
  <div class="x_title">
    <h3>About
      <ul class="nav navbar-right panel_toolbox">
        <li><a class="collapse-link"><i class="fa fa-chevron-up"></i></a></li>
      </ul>
    </h3>

    <div class="clearfix"></div>
  </div>
  <div class="x_content">
    <% if @is_organizer %>
        <div>
          <%= best_in_place @conference, :description, as: :textarea, place_holder: "&lt;&lt;Conference Description&gt;&gt;" %>
          <hr/>
        </div>
    <% else %>
        <% if @conference.description.length > 250 %>
            <%= truncate(@conference.description, length: 250) %>
            <%= link_to ' ... Read more', '', class: "read-more-description-#{@conference.id}" %>
        <% else %>
            <%= @conference.description.blank? ? '' : @conference.description + '<hr />' %>
        <% end %>
    <% end %>

    <div class="text-center">
      <h3 class="navy-background light-grey-text">0 Interested</h3>
    </div>
    <br/>

    <% if (!@conference.start_date.blank? and !@conference.end_date.blank?) or @is_organizer %>
        <strong>Event Date:</strong>

                <div class="input-prepend input-group">
                  <span class="add-on input-group-addon"><i class="glyphicon glyphicon-calendar fa fa-calendar"></i></span>
                  <input name="reservation-time" id="reservation-time" class="form-control" type="text">
                </div>
        <br/>
    <% end %>

    <% if !@conference.location.blank? or @is_organizer %>
        <div><strong>Location:</strong> &lt;&lt;Conference Location&gt;&gt; </div>
        <br/>
    <% end %>

    <% if !@conference.url.blank? or @is_organizer %>
        <div><strong>Website:</strong> <%= best_in_place @conference, :url, place_holder: "&lt;&lt;Conference Website URL&gt;&gt;" %> </div>
    <% end %>

    <% if !@conference.phone.blank? or @is_organizer %>
        <div><strong>Phone:</strong> &lt;&lt;Conference Phone&gt;&gt; </div>
    <% end %>

    <% if !@conference.email.blank? or @is_organizer %>
        <div><strong>Email Address:</strong> <%= best_in_place @conference, :description, place_holder: "&lt;&lt;Conference Contact Email&gt;&gt;" %> </div>
    <% end %>
    <br/>
    <div>Invite People</div>
  </div>
</div>