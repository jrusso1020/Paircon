function resetIntroJS() {
    Cookies.set('first_conference', '0');        
    introJs().goToStepNumber(1).start();
}

function initializeIntroJS() {
    if (Cookies.get('first_conference') == 0) {
        $("#startButton").click();     
    }
}
